import React from 'react';
import ClockImage from '../../0_loader/images/clock/Clock-Front.png';

const Clock = (props) => {
    return (
        <img className='Clock' src={ ClockImage }/>
    );
}

export default Clock