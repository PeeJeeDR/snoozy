import React from 'react';

const SmallSectionTitle = (props) => {
    return (
        <p className='SmallSectionTitle'>
            { props.title }
        </p>
    );
}

export default SmallSectionTitle